data:
  root_folder: tests/data
  recordings: neuropixel.bin

recordings:
  dtype: int16
  sampling_rate: 30000
  n_channels: 10
  spatial_radius: 70
  spike_size_ms: 1
  format: long

preprocess:
  apply_filter: True

detect:
  temporal_features: 3
